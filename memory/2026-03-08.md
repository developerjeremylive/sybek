# Sybek Development - March 2026

## Active Features
- Collapsible code blocks in chat responses (minimized by default)
- Futuristic UI with purple/cyan gradients
- File context - LLM reads existing files before editing
- Context folders selection in Files tab
- MCP Tab in Sybek UI - manage HTTP/SSE MCP servers

## Today's Work - March 8

### mcporter Integration
- Added mcporter tab to Sybek UI MCP page
- Updated mcp-store.ts with daemon state and actions:
  - `daemonRunning`, `mcporterServers` state
  - `startDaemon()`, `stopDaemon()`, `loadMcporterServers()` actions
- Added API endpoints in kilocode-proxy/server.js:
  - POST /api/mcporter-daemon (start/stop/status)
  - GET /api/mcporter-servers (list configured servers)
  - GET /api/mcporter-list (list with status)
- Updated MCPPage.tsx with new mcporter tab UI:
  - Daemon toggle button
  - Server list display
  - keepAlive config hint

### Config
- mcporter.json configured with keepAlive: true for core servers:
  - github, brave-search, firecrawl, filesystem, fetch, search

## GitHub Repos
- https://github.com/developerjeremylive/sybek/
- https://github.com/developerjeremylive/kilocode-proxy/

## API Endpoints
- Chat: https://kilicode-proxy-live.developerjeremylive.workers.dev/api/ai
- Tools: https://kilicode-proxy-live.developerjeremylive.workers.dev/api/tools
- mcporter: /api/mcporter-daemon, /api/mcporter-servers

## In Progress
- Build and test the new mcporter UI integration
- The daemon keepAlive feature needs verification (mcporter daemon start not recognizing keepAlive)

## Issues
- mcporter daemon requires keepAlive servers but they're not being detected
- Build pending to test UI changes
